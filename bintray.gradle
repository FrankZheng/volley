apply plugin: 'com.novoda.bintray-release'

publish {

    def groupProjectID = 'com.xqlabserv.volley'
    def artifactProjectID = 'xqlabserv-volley'
    def publishVersionID = '0.9.2'

    userOrg = 'frankzheng'
    repoName = 'Fork-Volley'
    groupId = groupProjectID
    artifactId = artifactProjectID
    publishVersion = publishVersionID
    desc = 'This is a fork of volley, which is a android network library, with some optimization.'
    website = 'https://github.com/FrankZheng/volley'
}

